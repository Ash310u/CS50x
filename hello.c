#include <cs50.h>
#include <stdio.h>

int main(void) {
    // string answer = get_string("What is your name? ");
    int num = 58;
    int answer = get_int("Guess a Number? ");
    if (answer < num)
    if(num == answer)
    {
        printf("Ta-dha ")
    }
    printf("hello, %s\n", answer);
   return 0;
}
