#include <stdio.h>
#include <string.h>
#include <cs50.h>

int define_grade(string text);
int main(void)
{
    string text = get_string("Text: ")

}

int define_grade(string text)
{
    int grade = 0;
    
}
