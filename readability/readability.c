#include <ctype.h>
#include <cs50.h>
#include <math.h>
#include <stdio.h>
#include <string.h>

int count_letters(string text);
int define_grade(string text);
int main(void)
{
    string text = get_string("Text: ");

}

int count_letters(string text)
{
    // Return the number of letters in text
}

int define_grade(int L S)
{

    int grade = 0.0588 * L - 0.296 * S - 15.8
}
